## 1. Guard source records and delete composition-name stripping

- [ ] 1.1 Add failing source tests: parameterized over `family`, `packageId` and `variant`, including null values, an entry on every ingestion path through shared normalization (RJNY, the BBoi34 standard and dual assets, the committed codm2000 catalog, and extras) fails ingestion with the source, entry and field identified and the error stating that the field cannot come from a source record and that composition policy in `config/composition.json` owns app families, package identities and per-pack selection, without directing the correction to composition policy; an RJNY entry excluded from export carrying a guarded field does not fail ingestion; a committed codm2000 record suppressed by dual coverage carrying a guarded field does fail; a formerly stripped top-level field such as `origin` on an upstream entry and on an extras entry, and an upstream `dualScreen`, reach the rendered record unchanged; `dualScreen` still sets extras eligibility and does not reach the rendered record. Delete the extras retired-field rejection test and the extras test expecting composition names to be stripped.
- [ ] 1.2 Implement the three-name guard once in shared normalization, remove `COMPOSITION_ONLY_FIELDS` from the modeled set and its import in `sources/common.py` while leaving the constant in `model.py` for the overlay, which still imports it, delete `_RETIRED_FIELDS` and its check in the extras adapter, and make the extras adapter consume `dualScreen` so it never reaches that entry's Obtainium record; confirm the focused source tests pass.
- [ ] 1.3 Add one CLI integration case where an extras entry carrying a guarded name fails the build and prior outputs remain intact. Pass the group's independent evidencing review and record the result in the change directory.

## 2. Align overlay, candidate-rule and denylist boundaries

- [ ] 2.1 Replace overlay protected-field tests so they cover exactly `id`, `url`, `overrideSource`, `family`, `packageId` and `variant`, each assigned and mapped to null, plus a control showing a formerly protected unread name is patchable; replace the legacy overlay object test with a non-array shape error that identifies the overlay. Implement the explicit six-name protected set without the model constant, then delete `COMPOSITION_ONLY_FIELDS` from `model.py` once no module imports it, and delete the legacy guidance; confirm the focused composition tests pass.
- [ ] 2.2 Replace retired names in candidate-rule unknown-field cases and in the policy rendered-record assertion with a generic unknown field and the fields composition actually owns, and make the denylist unknown-field cases generic; confirm policy and composition tests pass without code changes to rule or denylist parsing. Pass the group's independent evidencing review and record the result in the change directory.

## 3. Align CLI command, verification report and docs boundaries

- [ ] 3.1 Replace the retired verify flag test with a generic unsupported-flag case that exits nonzero before verification and leaves prior evidence intact; delete the `pack generate-source codm` `--force` rejection test in `tests/test_cli.py` without a replacement; confirm verification report schema-mismatch coverage in `pack report` and verification tests names no retired or old schema; confirm CLI, report and verification tests pass. Search source, tests and docs for remaining retired vocabulary and remove any that remains: `variants`, `dualPreferred`, `dual_preferred`, `eligible`, `selectionReason` and `selection_reason` as source-record or overlay field names, `COMPOSITION_ONLY_FIELDS`, `_RETIRED_FIELDS`, the legacy overlay guidance, the `pack verify` flags `--live` and `--probe-assets` and the `pack generate-source codm` flag `--force`, and retired or old verification schema wording. Composition-policy selector and rule fields (`source`, `origin`, `id`, `url`, `family`, `packageId`, `variant`), eligibility parameters and attributes, internal `App` attributes, report keys and prose uses of "variants" are out of scope.
- [ ] 3.2 Update `docs/composition.md`, `docs/development.md`, `docs/verification.md` and `docs/version-detection.md` to match the new behavior: `family`, `packageId` and `variant` on any source record fail ingestion and composition policy owns them; overlay protected fields are exactly `id`, `url`, `overrideSource`, `family`, `packageId` and `variant`; "Obsolete verification schemas" becomes a verification report with any other schema requiring regeneration with `pack verify`. Delete the retired-name and retired-flag passages, including the retired entries in the "Retired configuration" section, and keep generic unknown-field and unsupported-argument statements. Pass the group's independent evidencing review and record the result in the change directory.

## 4. Complete implementation validation and reviews

- [ ] 4.1 Confirm no current configuration file, captured upstream fixture, committed codm2000 catalog or committed pack carries `family`, `packageId` or `variant` at the top level of an app record; compose current configuration with identical captured inputs at the branch base and after implementation and confirm rendered bytes for both variants are identical, with no configuration migration. Record the result in the change directory.
- [ ] 4.2 Run the full suite and applicable repository checks, including offline pack verification and strict artifact validation; record results and confirm configuration, committed packs and workflows remain unchanged.
- [ ] 4.3 Complete the implementation workflow's independent whole-diff review wave and checkbox/evidence audit, resolve findings, rerun affected checks after fixes, and retain a self-contained validation record. Leave the change active and report the implementation branch.
