## 1. Coverage audit

- [x] 1.1 List every per-app fact that the deltas retire from pack-curation, readme-source-generation, source-ingestion, pack-cli and pack-composition. For each, record in `validation.md` in this change directory where reviewed configuration holds it. Where the fact is a composed outcome rather than a configured value, also record the test that checks it. A curated extra winning its family's single-screen selection is such an outcome; for every extra in the designated set (each `config/extras.json` entry that is eligible for single and whose family has no committed single pin), record the guard from 1.2 as its check. For a family that a single pin selects for, record the pin in `config/composition.json` as the reviewed selection instead. Verify that every fact maps to configuration or an outcome check.
- [x] 1.2 Rewrite the curated single-screen guard in `tests/test_port_curation.py` (`CURATED_SINGLE_WINNERS`, `curated_single_mismatches` and the tests that use them) so it derives the designated set from `config/extras.json` instead of a hand-kept list: every entry that does not set `dualScreen` to true and whose family has no single pin in `config/composition.json`. Derive each entry's family the way composition assigns it, so an extra that composition no longer selects still maps to its family: find the candidate rule in `config/composition.json` whose selector matches the entry (source `extras`, origin `extras`, the entry's id and its normalized URL); the family is that rule's `family`, or else `package:` followed by the rule's `packageId`, or by the entry's id when the rule sets no `packageId`; with no matching rule it is `package:<entry id>`. Equivalently, look the family up in the committed composition policy with the entry's effective id (the rule's `packageId` when it sets one, otherwise the entry's id) and its URL, never with an id that a rule corrects. The single-pin exclusion uses this same family, and the family's expected single-screen winner carries the effective id. Replace the mutation that makes an extra a dual-screen build, since that now removes it from the set, with a displacement that composition accepts and that keeps the extra designated: a temporary package denial, added to the denials the guard composes with, of a designated extra that no pin names, which as committed means Cinderbox or the omnipack tracker. Do not deny a dual-pinned extra's id, because composition rejects a denied pinned candidate before the guard compares anything. Verify that the guard passes as committed, that its derived set includes Cinderbox and the omnipack tracker, that an in-memory candidate rule giving a designated extra a `packageId` correction makes the derived family that rule's `family`, or `package:` plus the `packageId` when the rule sets no family, and that for each designated extra no pin names, composition succeeds under that extra's denial and the guard's result is exactly that extra's family, not a `CompositionError`. If an entry in the derived set does not win its family's single-screen selection as committed, stop and report it instead of editing configuration.
- [x] 1.3 For each composed outcome in 1.1 that no test checks once 1.2 is done, add a regression test over the committed configuration or fixtures. Verify each new test passes as committed and fails when the outcome is broken. If there are none, record that in `validation.md`.
- [x] 1.4 Search `openspec/specs/`, `docs/`, `tests/`, `config/`, `README.md` and `AGENTS.md` for all fifteen requirement names in this change's REMOVED blocks, and verify that none is cited outside its own spec file:
  - the eight retired outright, all in pack-curation: "Cinderbox is included in both packs", "Numeric app versions are selected without release labels", "Source-version tracking preserves build identity", "Curated store and established ports are present in both variants", "Xash3D tracks the continuous Android asset", "Hollow Knight entries have recognizable presentation", "Reviewed installed applications use verified identities and maintained sources" and "MetroidArch preserves a separate dual-screen Super Metroid selection";
  - the seven replaced under new names: "The upstream pack tracker is excluded" and "Curation regression checks protect exported configuration" in pack-curation; "Reviewed rules declare discovery and tracking treatment", "Package IDs are resolved automatically from release APKs" and "Explicit track-only resources remain honest non-APK entries" in readme-source-generation; "Committed codm2000 entries retain device-aware source semantics" in source-ingestion; and "A separate command generates the README source catalog" in pack-cli.

## 2. The codm2000 catalog: content-independent tests and generic tracker wording

- [ ] 2.1 Rewrite `test_committed_codm_entries_keep_their_source_semantics_in_composition` in `tests/test_source_generation_fixtures.py` so it builds a small fixture codm2000 catalog and composes it with the frozen captured higher sources. The fixture holds three entries:
  - an APK entry with prerelease settings and no higher coverage;
  - a track-only resource that extends an app a higher source provides;
  - an entry whose URL a dual-eligible higher-source candidate covers.

  Verify the test has an assertion for each of the source-ingestion scenarios "Dual coverage suppresses a local catalog candidate", "Newly resolved prerelease apps are admitted" and "A tracking resource keeps its identity", names no committed project, and passes.
- [ ] 2.2 List every test that reads `config/catalogs/codm.json`, and confirm that each can fail only where composition, build or verification with the committed configuration, or catalog validation as pack-curation defines it, would also reject the catalog. Catalog validation checks only that the catalog is an object with an apps list, its entry ids are unique, no two entries share a normalized project URL, each entry's id and flags suit its kind, and its bytes are the canonical rendering of its entries; a test asserting particular committed ids or projects does not count as catalog validation. Ingestion, reached from `pack build` through the codm2000 source's `fetch`, checks the first two. Source generation rejects duplicate ids and duplicate normalized project URLs in the committed catalog it reads before staging, rejects duplicate ids in its candidate and renders that candidate canonically. Composition does not load the catalog. For the staged file, only `test_committed_catalog_entries_have_unique_ids_and_urls` checks that normalized project URLs are unique, only the canonical-bytes test checks its canonical bytes, and only the kind-appropriate ID and flag test checks its entries' kinds. They are where the workflow currently performs those three checks, so they stay with their assertions intact. Then build a temporary candidate catalog that is an object with an apps list, rendered canonically, with unique ids, unique normalized project URLs and kind-appropriate IDs and flags:
  - remove at least two entries that no reviewed configuration references;
  - change one unreferenced entry's resolved package id.

  Confirm that the unique-ids-and-URLs, canonical-bytes and kind tests, `uv run pack build` and `uv run pack verify` accept the candidate, then verify that the full test suite passes with it in place. Restore the catalog from a copy saved before the candidate run, and verify that `config/catalogs/codm.json` is byte-identical to that copy and that nothing else under `config/` changed during the run.
- [ ] 2.3 In `effective_settings` in `src/omnipack/source_generation.py`, change the sentence appended to every track-only description to "Obtainium only tracks release notifications; acknowledgement does not install the resource or detect its installed version." In the same change, rewrite the `about` setting of the Kanto Gear entry (id `1845280017`) in `config/catalogs/codm.json` to exactly what the changed generator renders for its committed rule in `config/codm-projects.json`, keeping the file's canonical rendering. Do not edit `dist/`; the nightly rebuild carries the wording into the dual-screen pack. Verify, and record the results in `validation.md`:
  - rendering each committed track-only rule (only Kanto Gear today) through `effective_settings` yields settings that the committed entry with that rule's tracker ID carries with identical values, `about` included. Run this as a one-off check rather than a permanent test, because a permanent test asserting a committed catalog entry would fail on catalogs that catalog validation accepts;
  - the assertion in `tests/test_source_generation_boundaries.py` that a generated track-only description contains "acknowledgement does not install" still passes, and so does the canonical-bytes test;
  - compared with the catalog before this task, every other entry is unchanged, and the Kanto Gear entry differs only in its `about` text, where "the mod" becomes "the resource".

## 3. Main spec Purpose

- [x] 3.1 Rewrite the `## Purpose` of `openspec/specs/pack-curation/spec.md` directly, using the text in design.md: curated app decisions are recorded, protected and documented, plus the pack's own notification tracker and the exclusion of upstream pack trackers. Verify that `openspec validate --specs --strict` passes.

## 4. Checks and records

- [ ] 4.1 Verify that a search of the delta specs for the retired app and project names finds them only inside REMOVED blocks.
- [ ] 4.2 Run `openspec validate generalize-curation-specs --strict` and `just check-all`. Verify that both pass. Compared with the commit the branch started from, verify that under `src/` only `src/omnipack/source_generation.py` changed, and only in the track-only sentence from 2.3; that under `config/` only `config/catalogs/codm.json` changed, and only in the Kanto Gear entry's `about` text; and that `dist/`, `README.md` and `scripts/` are unchanged.
- [ ] 4.3 Complete `validation.md` in this change directory. It records the coverage audit, the derived single-screen set from 1.2, the candidate-catalog run from 2.2, the track-only wording check from 2.3, the check results, and test and spec line counts before and after.
