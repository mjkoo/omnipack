## 1. Guard the ingestion rules this change states

- [x] 1.1 Add characterization tests for the rules the ingestion deltas state for the first time: each configured BBoi34 asset pattern matching no asset, and matching two, fails the build naming BBoi34 and that pattern rather than selecting one; the committed codm2000 catalog repeating an entry id fails ingestion naming the id and both project URLs, while an upstream catalog contributing two entries that share a package id retains both for composition; two links to one non-GitHub host and path that differ only in a query, a fragment or a port are different projects, while two github.com links differing only in a query or fragment are the same project and two github.com links differing only in an explicit port are different projects; and a GitLab entry whose URL spells its host with a `www.` prefix, or carries a query, a fragment, credentials, an explicit port, a path component that is the reserved separator, or more path components than a project and its namespaces, fails with the entry and the URL identified, the `www.` case failing even though that URL compares equal to the canonical spelling under normalized comparison, while the canonical project URL is accepted and so is the same URL with its host spelled `https://GitLab.com/<group>/<project>`, since scheme and host are compared without regard to case.
- [x] 1.2 Confirm every test from 1.1 passes against unmodified source, since the deltas state shipped behavior and this change adds no implementation. Where a test does not pass, correct the delta to match the code rather than changing the code, and record which delta was corrected and why. Then show each test is able to fail: temporarily break the rule it guards in a scratch edit to `src/`, confirm the test fails for the stated reason, and revert the edit before going on, so no mutation reaches the diff. Record the mutation used for each rule.
- [x] 1.3 Read the four modified `source-ingestion` requirements and the rename against the modules that implement them, confirming that every SHALL stating system behavior in the restated text is exercised by an existing or new test and that no scenario name changed. The maintainer's obligation to supply a GitLab extra's package id is the one restated SHALL the pipeline does not check, so no test exercises it. Pass the group's independent evidencing review and record the result in the change directory.

## 2. Guard the composition rules this change states

- [x] 2.1 Add characterization tests for the composition rules stated for the first time: a candidate rule selector and a pin selector each naming a source with an origin belonging to a different source fail with the selector and the invalid origin identified, before any candidate is matched; an overlay record carrying a field other than `id`, `url` and `patch` fails with the record and the unknown field identified; an overlay record whose `id` or `url` is blank or not a string fails with the record and the offending field identified, without naming the value; and an overlay record whose `url` is a nonempty string no host can be read from, such as `/owner/repo`, fails with the record, the field and the offending value identified.
- [x] 2.2 Confirm the tests from 2.1 pass against unmodified source, correcting the delta to match the code rather than changing the code where one does not and recording which delta was corrected and why, and confirm the existing suite already covers the fallback rule carried into the union and precedence requirement: dual selects an eligible ordinary candidate when successful ingestion leaves no preferred candidate and no pin requires one, and a source that cannot be fetched aborts the build instead. Add the missing case if either is uncovered. Then show each test from 2.1 is able to fail: temporarily break the rule it guards in a scratch edit to `src/`, confirm the test fails for the stated reason, and revert the edit before going on, so no mutation reaches the diff. Record the mutation used for each rule.
- [x] 2.3 Confirm the retirement is safe: search source, tests and docs for anything relying on the retired requirement's prohibition of a post-build release metadata lookup, and confirm no module performs such a lookup and that the scenario carried forward appears once, under the union and precedence requirement, with its original name. Pass the group's independent evidencing review and record the result in the change directory.

## 3. Correct the generation contract

- [x] 3.1 Add characterization tests for the generation rules this change corrects and states: a rule enabling prereleases resolves from the bounded releases list and makes no request to the stable latest release, and a rule doing neither resolves from the stable latest release and makes no request to the list, in each case exactly one release lookup per project against the endpoint its rule selected, and a retryable transport failure of that lookup reissues it to that same selected endpoint and never to the other; a bounded releases response containing no release the rule permits fails identifying that limitation; a releases response holding more entries than the bound fails identifying the bound, distinguishably from the previous case; and a track-only rule carrying APK filename filtering, version extraction or its group selector fails validation with the project and setting identified, while a track-only rule enabling prereleases or consumer fallback is accepted and the generated entry carries the configured value. Add a characterization test for the accepted-catalog rule this change states for the first time: an accepted catalog carrying two entries with different ids whose project URLs normalize to the same form fails generation naming that normalized project.
- [x] 3.2 Confirm the tests from 3.1 pass against unmodified source, correcting the delta rather than the code where one does not, and confirm the corrected prerelease scenario no longer asserts a 404 from an endpoint the generator never requests. Then show each test from 3.1 is able to fail: temporarily break the rule it guards in a scratch edit to `src/`, confirm the test fails for the stated reason, and revert the edit before going on, so no mutation reaches the diff. Record the mutation used for each rule.
- [x] 3.3 Read all three modified `readme-source-generation` requirements against release selection, policy validation and accepted-catalog validation, confirming every restated SHALL stating system behavior is exercised and no scenario name changed. Pass the group's independent evidencing review and record the result in the change directory.

## 4. Take the mechanism and the per-app data out of curation

- [x] 4.1 Confirm, before the tracker's per-app data leaves normative text, that the tracker regression check asserts the rendered tracker's identity, its release-title pattern, its notification settings and its exact rendered key set against reviewed configuration, and that it fails when those configured values change. Add here whatever part of that assertion is missing, and record what was confirmed and what was added.
- [x] 4.2 Confirm the de-mechanized curation requirement still has a guard behind each outcome it now states rather than prescribes: that a maintained override survives a source refresh, and that a designated curated extra failing to win single fails a check identifying its family with coverage following from reviewed configuration. Confirm each catalog validity rule the requirement cross-references to a pipeline stage is stated in the capability that actually enforces it and is exactly what that stage checks: that a build rejects a malformed committed catalog and one repeating an entry id, both stated in `source-ingestion`; and that a malformed catalog document, a repeated entry id and a repeated normalized project URL are stated in `readme-source-generation` and are the whole of what generation checks when it reads the accepted catalog back, with the repeated normalized project URL checked by no ingestion adapter and no remaining curation text implying a build-time check that does not exist. Confirm the two rules the requirement now owns as suite obligations are guarded and are not attributed to the pipeline: a track-only entry whose id is not synthetic and numeric, or whose version detection, zip inclusion or architecture filtering is not disabled, and an APK entry whose id is not a well-formed package id or that carries the track-only flag, each fail the suite; and the committed catalog file's bytes are asserted equal to the canonical rendering of its entries. Confirm no remaining curation text names a test file, a fixture or a check's construction.
- [x] 4.3 Search the other main specs, the README and `docs/` for text that restates the curation mechanism, the tracker's per-app values or the retired composition requirement, and reconcile anything that now contradicts these deltas. Pass the group's independent evidencing review and record the result in the change directory.

## 5. Complete implementation validation and reviews

- [x] 5.1 Confirm this change altered no shipped behavior: compose the committed configuration over the suite's captured upstream records at the branch base and after implementation and confirm both variants render identical bytes, and confirm `src/` and `scripts/` are unchanged by the diff.
- [x] 5.2 Run the full suite and the applicable repository checks, including offline pack verification and strict artifact validation over both the change and the main specs; record the results in the change directory.
- [ ] 5.3 Complete the implementation workflow's independent whole-diff review wave and the checkbox and evidence audit, resolve findings, rerun affected checks after fixes, and retain a self-contained validation record. Leave the change active and report the implementation branch.
