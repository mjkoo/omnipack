## 1. Guard the ingestion rules this change states

- [ ] 1.1 Add failing tests for the rules the ingestion deltas state for the first time: each configured BBoi34 asset pattern matching no asset, and matching two, fails the build naming BBoi34 and that pattern rather than selecting one; the committed codm2000 catalog repeating an entry id fails ingestion naming the id and both project URLs, while an upstream catalog contributing two entries that share a package id retains both for composition; two links to one non-GitHub host and path that differ only in a query, a fragment or a port are different projects, while two github.com links differing only in a query or fragment are the same project; and a GitLab entry whose URL carries a query, a fragment, credentials, a port, a path component that is the reserved separator, or more path components than a project and its namespaces fails with the entry and the URL identified, while the canonical project URL is accepted. Confirm each test fails for the stated reason before any other change.
- [ ] 1.2 Confirm every test from 1.1 passes against unmodified source, since the deltas state shipped behavior and this change adds no implementation. Where a test does not pass, correct the delta to match the code rather than changing the code, and record which delta was corrected and why.
- [ ] 1.3 Read the four modified `source-ingestion` requirements and the rename against the modules that implement them, confirming that every SHALL in the restated text is exercised by an existing or new test and that no scenario name changed. Pass the group's independent evidencing review and record the result in the change directory.

## 2. Guard the composition rules this change states

- [ ] 2.1 Add failing tests for the composition rules stated for the first time: a candidate rule selector and a pin selector each naming a source with an origin belonging to a different source fail with the selector and the invalid origin identified, before any candidate is matched; an overlay record carrying a field other than `id`, `url` and `patch` fails with the record and the unknown field identified; and an overlay record whose `url` is not a project URL, or whose `id` or `url` is blank, fails with the record and the offending value identified. Confirm each fails for the stated reason.
- [ ] 2.2 Confirm the tests from 2.1 pass against unmodified source, and confirm the existing suite already covers the fallback rule carried into the union and precedence requirement: dual selects an eligible ordinary candidate when successful ingestion leaves no preferred candidate and no pin requires one, and a source that cannot be fetched aborts the build instead. Add the missing case if either is uncovered.
- [ ] 2.3 Confirm the retirement is safe: search source, tests and docs for anything relying on the retired requirement's prohibition of a post-build release metadata lookup, and confirm no module performs such a lookup and that the scenario carried forward appears once, under the union and precedence requirement, with its original name. Pass the group's independent evidencing review and record the result in the change directory.

## 3. Correct the generation contract

- [ ] 3.1 Add failing tests for the generation rules this change corrects and states: a rule enabling prereleases resolves from the bounded releases list and makes no request to the stable latest release, and a rule doing neither resolves from the stable latest release and makes no request to the list, in each case exactly one release request per project; a bounded releases response containing no release the rule permits fails identifying that limitation; a releases response holding more entries than the bound fails identifying the bound, distinguishably from the previous case; and a track-only rule carrying APK filename filtering, version extraction or its group selector fails validation with the project and setting identified, while a track-only rule enabling prereleases or consumer fallback is accepted and the generated entry carries the configured value.
- [ ] 3.2 Confirm the tests from 3.1 pass against unmodified source, correcting the delta rather than the code where one does not, and confirm the corrected prerelease scenario no longer asserts a 404 from an endpoint the generator never requests.
- [ ] 3.3 Read both modified `readme-source-generation` requirements against release selection and policy validation, confirming every restated SHALL is exercised and no scenario name changed. Pass the group's independent evidencing review and record the result in the change directory.

## 4. Take the mechanism and the per-app data out of curation

- [ ] 4.1 Confirm, before the tracker's per-app data leaves normative text, that a regression check asserts the rendered tracker's identity, its notification settings and its exact rendered key set against reviewed configuration, and that it fails when those configured values change. If that check is absent from the working branch, rebase onto the branch carrying it or add it here; record which was done.
- [ ] 4.2 Confirm the de-mechanized curation requirement still has a guard behind each outcome it now states rather than prescribes: that a maintained override survives a source refresh, and that a designated curated extra failing to win single fails a check identifying its family with coverage following from reviewed configuration. Confirm the catalog validity rules the requirement now cross-references are stated where they are enforced, and that no remaining curation text names a test file, a fixture or a check's construction.
- [ ] 4.3 Search the other main specs, the README and `docs/` for text that restates the curation mechanism, the tracker's per-app values or the retired composition requirement, and reconcile anything that now contradicts these deltas. Pass the group's independent evidencing review and record the result in the change directory.

## 5. Complete implementation validation and reviews

- [ ] 5.1 Confirm this change altered no shipped behavior: compose the committed configuration over the suite's captured upstream records at the branch base and after implementation and confirm both variants render identical bytes, and confirm `src/` and `scripts/` are unchanged by the diff.
- [ ] 5.2 Run the full suite and the applicable repository checks, including offline pack verification and strict artifact validation over both the change and the main specs; record the results in the change directory.
- [ ] 5.3 Complete the implementation workflow's independent whole-diff review wave and the checkbox and evidence audit, resolve findings, rerun affected checks after fixes, and retain a self-contained validation record. Leave the change active and report the implementation branch.
