## 1. Maintainer editing guidance

- [x] 1.1 Add common-edit recipes in `docs/development.md` for extras, codm project policy, overlays, denials, identity corrections, family grouping and pins; verify each field and selector against its current parser and link to the owning semantics reference, confirming the recipe marks `categories` strongly recommended with its heading, sort and `Other` fallback. Repair the overlay example in the linked composition reference so a patch merges keys into the settings object instead of encoding them as a string, and verify the repaired example renders.
- [x] 1.2 Document manual generation when needed, candidate review and acceptance, build, output/report review, offline verification and same-PR changed outputs; verify an ordinary edit, a codm policy edit and a successful no-op build each have an unambiguous path, without invoking automation's commit-producing staging script. Verify that an overlay-only edit's recipe rests on the `dist/` and README diff against the PR base rather than the report's `changes`; that the no-op path accepts a build only once the report shows the edit took effect - a denial's package id present in `denylistRemovals` and absent from `staleExclusions`, a pin or rule's intended winner in `selections` - or the edit was expected to be inert; and that the documented response to main's generated files moving under an open PR is to rebase onto main, rerun generation where applicable and `uv run pack build`, then re-review, with conflicts in the two pack files and in the region between the README's catalog markers resolved by rebuilding rather than by hand while a README conflict outside those markers is merged by hand and re-read, and a codm catalog conflict resolved by taking main's catalog as the base, re-running generation, re-inspecting the candidate and diagnostics, re-copying it per the acceptance recipe and building, never by hand-resolving it or keeping main's catalog.
- [x] 1.3 Explain the automated catalog-only PR exception and correct the main-CI comparison in `docs/source-generation.md`; verify the prose against both CI workflows and the source proposal staging allowlist, preserving the existing workflow contract.

## 2. Documentation drift repairs

- [x] 2.1 Correct tracker publication and revision wording in `docs/curation.md`, delete the stale clause that a rollback's publication is a separate maintainer action, and repair the stable-download-URL claim in `docs/publishing.md`; verify the curation wording matches the upload/edit and repair branches in `scripts/nightly_write.py` and the rolling-release spec, keeps the non-atomic visibility limit to one line plus an added publishing-page link, and makes no post-upload readback or completed-device-acceptance claim. Verify that the publishing page carries the full statement that the URLs serve whatever the most recent upload left in place, with the raw-main links as the fallback until a later run completes or repairs the pair, and that the curation page's rollback sentence keeps only restoring the previous extras and overlay and rebuilding both files, asserting nothing about when or how a rollback reaches the release. Change no publication behavior, specs or workflows.
- [x] 2.2 Restate Ludashi wording in `docs/composition.md` and `docs/curation.md` as present-tense facts, each on exactly one page; verify the composition page keeps the release selection and source-version policy, neither credits a past effort, and Ludashi's decision and precautions stand.

## 3. Validation and implementation review

- [x] 3.1 Validate illustrated records with current parsers or existing fixture-backed checks, covering examples in the references the recipes link to as well as examples inlined here, and confirm the repaired overlay example patches `additionalSettings` as an object; check the corrected report-command wording, confirming it links to the verification page for the fingerprinted set instead of re-enumerating it and says edits outside that set leave evidence current until a rebuild changes the outputs; inspect CLI help for documented commands, run `just check-links`, `uv run pack verify` and `git diff --check`, and record results in this change's validation document; confirm no production code, config, workflows, pack outputs or README were changed by the documentation work.
- [x] 3.2 Complete required per-group evidencing reviews, the independent final review wave, any fix/review rounds and the checkbox completion audit; record evidence for every task in this change before marking implementation complete. Do not invoke the separate OpenSpec verify or archive workflows as part of this task.
