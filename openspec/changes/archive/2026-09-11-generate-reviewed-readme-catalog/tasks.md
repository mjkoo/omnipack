## 1. Capture migration evidence and source boundaries

- [x] 1.1 Inventory eligible README table projects, previously admitted generated candidates and higher-source coverage using captured inputs; record an explicit mapping to existing package-ID cache entries and confirm the baseline pack bytes and family winners are reproducible.
- [x] 1.2 Define the accepted catalog/metadata/resolution-state fixtures and candidate output layout; verify fixtures cover a new project, accepted fallback, covered dual project, single-only coverage, removal and conflicting package IDs without modifying protected historical evidence.
- [x] 1.3 Extend evidence with captured prerelease metadata and manifest observations for EmuLnk, Showdown-DS and Heimdall, Kanto's non-APK mod and official installation path, and reviewed project-policy fixtures; verify policy hashes, explicit tracking identities and a separate expected-additions regression without rewriting the completed 92/109 baseline or historical ledgers.

## 2. Implement automatic source generation

- [x] 2.1 Separate README table parsing from build ingestion and add reviewed APK/track-only project policy before discovery; verify table-scoped parsing, normalized duplicate links and policy keys, supported consumer fallback settings, unsupported/inactive rules, invalid types/regexes/combinations, malformed/empty tables, stable rendering and collisions across APK and tracker IDs.
- [x] 2.2 Integrate the APK resolver with candidate state, accepted membership and effective-policy fingerprints; verify stable-default and bounded prerelease/title selection, draft exclusion, deterministic ordering, scan-limit failure, filename-filter diagnostics, agreement across every eligible APK, unchanged release/policy reuse, changed release or policy reinspection, bounded downloads, credential scoping and no generator older-release or incompatible-policy fallback, including a newer Heimdall debug title and a newest matching release with no eligible APK while an older matching release is usable.
- [x] 2.3 Add the source URL/README/policy hash gate, catalog digest and manual force behavior; verify policy validation before no-ops, zero-resolution unchanged-input runs, policy-only and metadata-only changes, formatting-only reuse, release-only forced refresh, unchanged-content no-ops and retries while inputs remain unaccepted.
- [x] 2.4 Generate explicit track-only resources with stable IDs and manual-installation descriptions; verify Kanto's metadata-only path, no APK/archive requests or cache entries, new-tracker failure, unchanged-policy accepted fallback, no automatic conversion after failed APK resolution, kind-transition validation and absence of embedded release/installed-version state.
- [x] 2.5 Expose `pack generate-source codm [--force]` with current-run diagnostics and isolated `.build/` candidate outputs; verify exit statuses, distinct APK/tracking outcomes, stale-artifact rejection and that success/failure cannot mutate tracked catalogs, policy, cache, pack outputs or git history.

## 3. Migrate routine builds to the accepted JSON source

- [x] 3.1 Commit the four reviewed project rules and automatically generate the initial complete catalog and bound metadata/state, using the documented legacy default-policy baseline only for previously admitted APK projects; verify Showdown and Heimdall resolve from prerelease APKs, Heimdall alone enables consumer older-release fallback while Showdown and EmuLnk explicitly disable it, EmuLnk resolves independently, Kanto validates as track-only, every eligible project is accounted for and no partial seed or manually supplied APK ID is accepted.
- [x] 3.2 Replace codm build-time scraping with committed JSON normalization while preserving generated provenance, identity, dual coverage filtering and preference; verify byte-identical controlled exports without the requested additions, then only the two APKs and Kanto tracker added to dual with existing app bytes and winners unchanged and single unchanged; cover correct EmuLnk suppression/settings, higher-source eligibility overrides, normalized duplicate URLs, single-only coverage and existing selectors.
- [x] 3.3 Remove the build's resolver/cache construction and move resolution-attempt diagnostics out of its report; verify normal builds make no README/APK requests, never mutate resolver state, fail on invalid local catalogs and preserve composition diagnostics and supported report compatibility.
- [x] 3.4 Remove resolution state from nightly candidate capture, staging and commit allowlists; verify cache/source/metadata/policy mutations are rejected while existing exact-byte, README-boundary, no-op, main-advancement and release synchronization tests pass.

## 4. Add checked catalog-update automation

- [x] 4.1 Implement one candidate-check operation using the selected main base, source and policy checks, composed pack build and structural verification; verify stale selectors, APK/tracker collisions, failed builds and changed checked source or policy bytes block publication, and only catalog/metadata/state enter the source commit while policy remains read-only and pack changes remain diagnostics.
- [x] 4.2 Implement the dedicated owned branch/PR lifecycle with source-only writes and one attempt; verify with controlled git/remote transports initial creation, same-content no-op, updated content, main/branch races, foreign changes, previous merged/closed PRs and ambiguous-write discovery on a later run without duplicate creation.
- [x] 4.3 Wire daily and manual forced runs, serialized execution, canonical-main gating, locked setup and scoped token permissions; verify workflow boundary tests and action linting cover fork/ref restrictions, explicit checks before PR writes, no main/release/issue operations and no implicit dependence on PR-triggered CI.
- [x] 4.4 Add current-run summaries and 14-day diagnostic artifacts; verify generation, validation, PR and early setup failures remain visible without stale success, leaked credentials, executed source text or archived APK/HTTP bodies.

## 5. Documentation and integrated verification

- [x] 5.1 Update development, ingestion, curation and publishing guidance for committed source inputs, reviewed per-project settings, automatic APK resolution, explicit tracking resources, README/policy hash acceptance, forced refresh, fallback retry timing and PR permission/check prerequisites; document Kanto notifications versus Gen1Recomp mod installation and verify active docs no longer promise nightly APK discovery or mod installation by Obtainium, with no durable scratch references.
- [x] 5.2 Run required development checks, workflow linting and controlled two-run tests for unresolved-then-successful generation, policy-only changes, open-PR updates and accepted-catalog nightly independence; verify Obtainium prerelease asset/version selection and Kanto import/tracking without APK installation, record exact existing-entry preservation plus intended additions, and perform no real remote writes.
- [x] 5.3 Complete required implementation reviews and task-evidence audit, verify all capability deltas, and synchronize specs through the archive workflow; verify strict change/main-spec validation, preserve historical ledgers, and leave the implementation branch available without pushing or merging.
