## REMOVED Requirements

### Requirement: Public GitLab entries retain native source identity

**Reason**: Moved intact to its pipeline capability; behavior is unchanged.

**Migration**: Use "Public GitLab entries retain native source identity" in `source-ingestion`.

### Requirement: GitLab release resolution includes uploaded description APKs

**Reason**: Moved intact to its pipeline capability; behavior is unchanged.

**Migration**: Use "GitLab release resolution includes uploaded description APKs" in `pack-verification`.
