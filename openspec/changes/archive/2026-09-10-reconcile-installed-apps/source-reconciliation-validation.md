# Source reconciliation validation

This records the preceding reconciliation snapshot. The later MetroidArch addition
is documented in [MetroidArch validation](../2026-09-10-include-metroidarch/metroidarch-validation.md); counts and
hashes below describe the earlier output, not the current pack.

Validation date: 2026-09-10 UTC. Verifier 0.7.0 retains the GitHub/HTML
Obtainium 1.6.14 compatibility baseline and the existing GitLab boundary.

## Generated output

`uv run pack build` fetched configured source catalogs and generated both packs
and their README catalog. `uv run pack verify` passed the exact generated bytes
and maintained policies. The pack counts are 92 single-screen and 111 dual-screen.

The output comparison against the preceding pack found only the approved
identity corrections, official Ghostship replacement, Super Metroid exclusion,
canonical Gen1Recomp URL, and explicit source-version detection for dual CTR.
Other retained source and asset-selection settings remain unchanged.

| Artifact | SHA-256 |
| --- | --- |
| `dist/single-screen.json` | `f55841cc37706d93256f67b679c9b61eaf52f9fe05f61bf285cb3d0c8626f56f` |
| `dist/dual-screen.json` | `591670a1f6d782f4f636760f695d4b84efb6326a82aacb63750435a54be1b8e3` |
| `README.md` | `f03cb3d5fe113278dbb11ba75b566d39b7104d8bac4feb82bd2fba7c7581a429` |

## Official Ghostship probe

A fresh resolver check selected release `3.0.0` and only
`Nautilus-Alfa-Android.zip`. The explicit bounded GET returned HTTP 206 with
1024 bytes read. The separate APK inspection established member `Ghostship.apk`,
package `dev.net64.ghostship`, and versionName `3.0.0`; runtime live verification
continues to warn that archive members are not inspected.

## Device acceptance boundary

The comparison covers all 35 actual Android packages managed in the captured
pre-import export; four additional entries in the 39-entry installed-only export
were track-only resources. The two original exports and Android package inventory
were preserved before any pack import.

CTR needs a fresh installation for its signing-key switch; official Ghostship
uses a different package; OpenMW-DS can receive an ordinary same-signer update;
retired Super Metroid is excluded without automatic device removal. Tracking-ID
corrections alone do not imply reinstalls. The comparison does not constitute
on-device validation. No uninstall, install, save/controller migration, import,
or publication was performed during this update.

## Complete live verification

The final `uv run pack verify --live` completed successfully with frozen inputs
from 2026-09-10T17:10:16.252435+00:00 to 2026-09-10T17:14:10.803898+00:00. It covered all
203 entries (92 single-screen, 111 dual-screen), with zero errors and two expected
`archive-members-unverified` warnings, one for Ghostship in each variant.
This was ordinary metadata verification; the bounded Ghostship asset probe above
was a separate diagnostic.

All 33 retained managed apps resolved. Thirty selected the same effective source
version as the captured installed export after normalizing Banjo/Symphony numeric
extraction and Cemu's release-title-to-tag policy. The three version transitions
were CTR, official Ghostship, and OpenMW-DS. Super Metroid was removed and Obtainium
itself remains outside the pack. No additional APK replacement was indicated by
these version checks; byte identity was not tested for every app in the pack.

The canonical Gen1Recomp source selected `v0.2.57` and the exact previously
inspected APK URL. A fresh GitHub asset lookup returned ID `552977054` and SHA-256
`6fc6129ea13d9fc4c7ba57793a0294d45adf50944fccc433fd84fa37ac11f2b1`, agreeing with
the previously inspected and installed APK bytes.

## Automated checks

The baseline suite passed 958 tests. New ZIP tests first failed against the
unsupported-archive implementation, then passed after the resolver change.
The final pre-review suite passed 975 tests. Ruff lint/format checks, ty,
`uv lock --check`, and Python source/wheel packaging also passed.

Captured independent catalog inputs exercise the maintained correction rules
through two refreshes, including exact retained IDs, target-specific CTR sources,
family/package uniqueness, official Ghostship selection, canonical Gen1Recomp,
and retired-family exclusion. Separate APK observations connect corrected IDs
to inspected release assets. ZIP tests cover enabled/disabled selection, outer
platform filtering and inversion, fallback, member-regex preflight, bounded outer
probing, explicit member limitations, and unchanged HTML/GitLab archive rejection.
